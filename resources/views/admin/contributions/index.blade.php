<x-admin-layout title="Expert contributions">
    <a href="{{ route('admin.dashboard') }}" class="text-sm text-slate-500 hover:underline dark:text-slate-400">← Back to dashboard</a>
    <div class="mt-2 flex flex-wrap items-end justify-between gap-3"><div><h1 class="text-xl font-bold text-slate-900 dark:text-white">Expert contributions</h1><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Triage suggestions before anything enters the governed question library.</p></div><form method="GET"><label for="status-filter" class="block text-xs font-semibold text-slate-500 dark:text-slate-400">Filter by status</label><select id="status-filter" name="status" onchange="this.form.submit()" class="mt-1 rounded-xl border border-slate-300 bg-white px-3.5 py-2.5 text-sm text-slate-900 shadow-sm focus:border-vytte-500 focus:outline-none focus:ring-2 focus:ring-vytte-500/20 dark:border-slate-600 dark:bg-slate-900 dark:text-white"><option value="">All statuses</option>@foreach ($statuses as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ str($status)->replace('_', ' ')->title() }}</option>@endforeach</select></form></div>
    <div class="mt-5 space-y-3">@forelse ($contributions as $item)<a href="{{ route('admin.contributions.show', $item->content_contribution_id) }}" class="section-card block p-5 hover:border-vytte-300 dark:border-slate-700 dark:bg-slate-800"><div class="flex items-start justify-between gap-3"><div><h2 class="text-sm font-bold text-slate-900 dark:text-white">{{ $item->title }}</h2><p class="mt-1 text-sm text-slate-600 dark:text-slate-300">{{ Str::limit($item->question_text, 180) }}</p><p class="mt-2 text-xs text-slate-400">{{ $item->workspace?->name }} · {{ $item->submitter?->name }} · {{ $item->created_at->diffForHumans() }}</p></div><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold dark:bg-slate-700 dark:text-slate-200">{{ str($item->status)->replace('_', ' ')->title() }}</span></div></a>@empty<x-empty-state title="No contributions" description="New expert suggestions will appear here." />@endforelse</div><div class="mt-4">{{ $contributions->links() }}</div>
</x-admin-layout>
