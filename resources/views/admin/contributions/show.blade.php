<x-admin-layout title="Review contribution">
    <a href="{{ route('admin.contributions.index') }}" class="text-sm text-slate-500 hover:underline">← Contributions</a>
    <div class="mt-2 flex flex-wrap items-start justify-between gap-3"><div><h1 class="text-xl font-bold text-slate-900 dark:text-white">{{ $contribution->title }}</h1><p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Submitted by {{ $contribution->submitter?->name }} from {{ $contribution->workspace?->name }}</p></div><span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold dark:bg-slate-700 dark:text-slate-200">{{ str($contribution->status)->replace('_', ' ')->title() }}</span></div>
    <div class="mt-5 grid gap-4 lg:grid-cols-3"><div class="space-y-4 lg:col-span-2"><div class="section-card p-6 dark:border-slate-700 dark:bg-slate-800"><p class="text-xs font-semibold uppercase tracking-wide text-slate-400">Proposed question</p><p class="mt-2 text-lg font-semibold text-slate-900 dark:text-white">{{ $contribution->question_text }}</p><dl class="mt-4 grid gap-3 text-sm sm:grid-cols-2"><div><dt class="text-slate-400">Answer format</dt><dd class="font-semibold text-slate-800 dark:text-slate-200">{{ str($contribution->response_format)->replace('_', ' ')->title() }}</dd></div><div><dt class="text-slate-400">Suggested area</dt><dd class="font-semibold text-slate-800 dark:text-slate-200">{{ $contribution->module?->module_name ?: 'Not specified' }}</dd></div></dl>@if ($contribution->answer_options)<p class="mt-4 text-sm text-slate-600 dark:text-slate-300"><span class="font-semibold">Choices:</span> {{ collect($contribution->answer_options)->join(' · ') }}</p>@endif</div><div class="section-card p-6 text-sm dark:border-slate-700 dark:bg-slate-800"><h2 class="font-bold text-slate-900 dark:text-white">Purpose and evidence</h2><p class="mt-3 text-slate-600 dark:text-slate-300">{{ $contribution->intended_use }}</p><dl class="mt-4 space-y-2"><div><dt class="text-xs text-slate-400">Source</dt><dd>{{ $contribution->source_authority ?: 'Not supplied' }}</dd></div><div><dt class="text-xs text-slate-400">Licence</dt><dd>{{ $contribution->license_code ?: 'Not supplied' }}</dd></div><div><dt class="text-xs text-slate-400">Method notes</dt><dd>{{ $contribution->methodology_notes ?: 'Not supplied' }}</dd></div></dl></div></div>
        <div class="space-y-4">@if ($contribution->status !== 'PROMOTED')<form method="POST" action="{{ route('admin.contributions.review', $contribution->content_contribution_id) }}" class="section-card space-y-3 p-5 dark:border-slate-700 dark:bg-slate-800">@csrf @method('PATCH')<h2 class="text-sm font-bold text-slate-900 dark:text-white">Review decision</h2><select name="status" required class="w-full rounded-xl border-slate-300 text-sm dark:border-slate-700 dark:bg-slate-900">@foreach (['IN_REVIEW','NEEDS_CHANGES','ACCEPTED','REJECTED'] as $status)<option value="{{ $status }}" @selected($contribution->status === $status)>{{ str($status)->replace('_', ' ')->title() }}</option>@endforeach</select><textarea name="review_notes" required rows="4" maxlength="5000" placeholder="Explain the decision and any changes needed" class="w-full rounded-xl border-slate-300 text-sm dark:border-slate-700 dark:bg-slate-900">{{ $contribution->review_notes }}</textarea><button class="w-full rounded-xl bg-vytte-600 px-4 py-2 text-sm font-semibold text-white">Save decision</button></form>@endif
        @if ($contribution->status === 'ACCEPTED')<form method="POST" action="{{ route('admin.contributions.promote', $contribution->content_contribution_id) }}" class="section-card space-y-3 p-5 dark:border-slate-700 dark:bg-slate-800">@csrf<h2 class="text-sm font-bold text-slate-900 dark:text-white">Promote to a governed draft</h2><p class="text-xs text-slate-500">Promotion does not publish. It creates a private draft that must pass ordinary content and scoring review.</p><select name="module_id" required class="w-full rounded-xl border-slate-300 text-sm dark:border-slate-700 dark:bg-slate-900"><option value="">Choose department</option>@foreach ($departments as $department)<option value="{{ $department->module_id }}" @selected($contribution->module_id === $department->module_id)>{{ $department->module_name }}</option>@endforeach</select><select name="content_publisher_id" required class="w-full rounded-xl border-slate-300 text-sm dark:border-slate-700 dark:bg-slate-900"><option value="">Choose accountable publisher</option>@foreach ($publishers as $publisher)<option value="{{ $publisher->content_publisher_id }}" @selected($contribution->proposed_publisher_id === $publisher->content_publisher_id)>{{ $publisher->name }} · {{ str($publisher->verification_status)->title() }}</option>@endforeach</select><button class="w-full rounded-xl bg-vytte-600 px-4 py-2 text-sm font-semibold text-white">Create draft question</button></form>@endif</div>
    </div>
</x-admin-layout>
